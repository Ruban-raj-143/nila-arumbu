# Nila Arumbu — Backend Dependencies
# Python 3.12+

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# Pydantic & settings
pydantic==2.10.3
pydantic-settings==2.6.1
pydantic[email]==2.10.3

# Auth & security
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.18

# Redis & caching
redis==5.2.1

# Celery (background tasks)
celery==5.4.0

# HTTP client
httpx==0.28.1
aiofiles==24.1.0

# Twilio (WhatsApp + SMS)
twilio==9.3.7

# Voice — Tamil Speech Recognition
# openai-whisper  # disabled — heavy PyTorch dep, install separately if needed

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
aiosqlite==0.20.0
